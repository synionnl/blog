type: object
properties:
  command:
      $ref: "types/command.yaml"
  question_id:
    $ref: "../types/question_id.yaml"
  rejection:
    $ref: "../types/rejection.yaml"
required:
  - command
  - question_id
  - rejection